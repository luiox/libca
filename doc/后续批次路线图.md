# 后续批次路线图

> 状态：规划（2026-09，随批次 1 收尾整理）。前置阅读：`doc/加密与编码内置化方案.md`。
> 原则：每批都是"可独立合入的完整增量"；**明确不做**清单与批次清单同等重要（防返潮）。

## 批次总览

| 批次 | 主题 | 规模 | 依赖 |
|------|------|------|------|
| 1（已合入） | 15 个小件 | ~6600 行 | 无 |
| 2（已交付） | 加密 P0 组合件 + varint + 配置中心 | ~1 周级 | 无 |
| 3 | 编码内置化 Tier1+GB18030、TlsStream 重构、SockUtil 增量 | 2~3 周级 | 无 |
| 4 | MessageLoop、IPC 容错、AES + 后端选择骨架 | 2~4 周级 | 批次 2 的 SHA-512（AES 可选） |
| 5 | 按需/远期（P2/P3 加密、event loop、SQLite、SMTP、WebSocket） | 需求驱动 | 各项内部注明 |

---

## 批次 1（已合入 main，PR #215）

15 个小件全量落地，分支 `feat/small-items`。

## 批次 2：快速组合件（已交付，分支 `feat/batch-2`）

三项全部完成，与原计划的差异两处：

1. **crypto P0**：SHA-512/SHA-384（照 sha256 结构）→ HMAC-SHA1/SHA512 补齐家族 →
   HKDF（RFC 5869）→ PBKDF2-HMAC-SHA256（RFC 8018）。✅ 全部落地，官方向量经
   python hashlib/hmac 交叉验证后入库。
2. **core：Bytes varint/zigzag**：`put_var_u32/u64`、`get_var_u32/u64`（Underflow 走
   现有 BytesError）、zigzag 编解码助手。✅ 落地；非规范编码新增
   `BytesError::MalformedVarint` 报错（原计划只复用 Underflow，实现时发现无法区分
   截断与坏数据，追加枚举值）。
3. **配置中心**：✅ 落地为新模块 `config`。与原计划差异：
   - 中间格式用 **JSON**（复用现有 json 模块 `JsonReader`）而非自写 YAML 片段解析器，
     免维护一个解析器，依赖 core/json/fs 纯向下；
   - 原计划的"落地后先接 log 级别热更新"**不做直接接线**：log 与 config 同属上层，
     库内直接依赖违反同层无依赖约束，热更新示例降级为文档用法（应用层自行桥接）。

## 批次 3：痛点清除

1. **str 编码内置化**（对应方案 §一）：
   - Tier 1：UTF-8/16/32 纯算法互转收拢（复用 utf8_util）、Latin-1、CP1252；
   - 表生成脚本（WHATWG index → `.inc` 入库）+ **GB18030 默认嵌入**，
     `CharsetConverter` 改三级查找：内置表 → iconv 回落 → UNSUPPORTED；
   - 验收标准：裁剪 glibc 环境 GBK 不再依赖 gconv；GBK 用例固定向量跨平台断言。
2. **TlsStream 重构**（对应方案：TLS 永远不内置协议，只做适配层）：
   - 从 http 层抽出 OpenSSL 逻辑 → `TlsStream`（mem-BIO 解耦，实现 io::Reader/Writer），
     能包 TcpStream / 命名管道 / 内存流（单测不起网络）；行为等价重构，http 回归兜底；
   - 难点预算主要给安全默认值：证书校验默认开、主机名验证、SNI、错误分类。
3. **net SockUtil 增量**：`get_local_ip()`（UDP connect 探路由）、TCP keepalive 三参数、
   `getInterfaceList()`、DNS 缓存（带 TTL，与 http 连接池配套）。组播**不在本批**
   （等设备发现需求）。

## 批次 4：结构增强

1. **thread MessageLoop**：线程亲和任务循环——`post_task` / `post_task_and_reply` /
   `post_delay_task`（延迟任务可落在批次 1 的 TimerManager 上）。这是 event loop 的
   前身，异步化路线第一块砖。
2. **process IPC 容错**：写者进程探活（崩溃后读者接管）、环形槽语义增强（Morn
   morn_message.c 思想，只学设计）。
3. **crypto P1：AES**：内置参考实现 AES-128/192/256 ECB/CBC/CTR（文档明示不承诺
   常量时间）+ `--with_openssl` / `--with_cng` 后端选择骨架 + 双实现对拍测试。
   AES-GCM 只做外部后端。随后可按需开 schannel（Windows 零依赖 TLS）后端。

## 批次 5：按需 / 远期（需求驱动，不主动排期）

| 项 | 触发条件 | 备注 |
|----|----------|------|
| ChaCha20-Poly1305 AEAD、X25519 | 出现"加密通道/协议"需求 | 两者 + HKDF 即 Noise 协议积木；核心可平移 em_crypto |
| ed25519（verify 先行） | 出现签名验证需求 | 前置 SHA-512（批次 2 已有） |
| event loop（IOCP/EventPoller） | 真实高并发需求（桌面场景通常不需要） | MessageLoop 骨架复用；sylar IOManager 只读不抄 |
| `ca::task<T>`（C++20 协程壳） | 升级 C++20/23 基线后 | 对齐"ArrayList 包 vector"的哲学；**不做栈式 Fiber** |
| SQLite 两层封装 | 真实本地存储需求 | 核心封装（prepare/bind/Result<Row>）+ 显式映射 CRUD 助手；**不做 ORM 代码生成** |
| SMTP 客户端 | 报警通知需求 | ~500 行 |
| WebSocket | 有真实场景且 TLS 已就绪 | 排在 TlsStream 之后 |
| zip 错误模型迁移 Result | 顺手的技术债 | 本批 gzip 跟随了模块现状（抛异常），全库统一时一并处理 |

## 技术债与工程事项（随手修，不占批次）

1. `.clang-format` 与 `prompt/code_rule.md`（K&R）冲突——现配置连存量代码都报违规，
   建议统一为规则文件口径；
2. xmake 在 worktree 内 projectdir 向上误解析到主 checkout（本批 8 个 agent 全踩过，
   还覆盖过主 checkout 的 gitignored 配置）——在 AGENTS.md 记一条注意事项，或调查
   xmake 配置根治；
3. gtest 在 mingw 下安装不稳（README 已有 msvc 变通），CI 保持 msvc 工具链。

## 明确不做（防返潮，讨论已结论）

- **Fiber / 栈式协程**：库偷偷转 fiber 模式对嵌入方是敌意行为；异步未来 =
  MessageLoop → event loop（按需）→ C++20 协程壳。
- **RSA / DES / RC4 新用途**：需要 RSA 时走 OpenSSL 后端。
- **零拷贝 Buffer 抽象 / ByteArray 链式架构**：Bytes 三件套已覆盖，varint 补齐即可。
- **ORM 代码生成**：C++17 无反射，工具链维护成本 > 收益。
- **util 大杂烩模块**：杂件按落位原则归现有模块（utility/reflect 前车之鉴）。
- **ICU / 自写 TLS 协议栈**：编码用 WHATWG 表自给，TLS 只做成熟后端适配层。
