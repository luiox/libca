
日志库应该由日志等级、日志记录器（Logger）、格式化器（Formatter）、过滤器（Filiter）、输出器（Outputter）、管理器（LoggerManager）组成。

其中日志记录器只负责收集日志信息，并且调用格式化器进行格式化，然后先写到一个缓冲区，之后再由输出器进行输出到不同的地方，管理器的作用是进行协调和管理这些东西，创建一个线程来读写IO。

最基本的应该是可以直接使用日志器和输出器进行组合使用。一个负责记录，一个负责输出。

但是并不是如此简单，
