# libca源码包管理方式细节文档（已对齐新版规范）

本文档不再独立定义规则。

`doc/libca源码包规范.md` 是唯一事实来源和最终实现目标。

## 当前状态

1. 本文档历史版本中涉及的自动依赖注入、默认port自动注入等描述已失效。
2. 所有实现与示例应以最新版 `doc/libca源码包规范.md` 为准。
3. 若本文件与规范文件存在冲突，以规范文件为准。

## 保留目的

1. 保留文件路径，避免外部引用失效。
2. 作为迁移提示入口，提醒维护者统一查看规范文件。
