# v0.2 手工测试计划（3 天）

**目标版本**：v0.2

**发布时间目标**：5 天后发布（当前第 N 天），本计划覆盖预计的 3 天手工测试工作。

---

## 📋 范围（包含模块）
- **em_base**：基础组件（包含现有已定稿内容）
- **em_test**：单元测试组件（新增断言宏、结构化输出 - console + 文件）
- **em_driver**：驱动模块
  - DHT11（温湿度）
  - TOFxxF（飞行时间测距模块）
  - DS1302（实时时钟）
  - W25QXX（SPI Flash）
  - AT24CXX（I2C EEPROM）

> 注：遵循 `prompt/em_code_rule.md` 的测试规范与风格要求（C99、`TEST_ENABLE` 条件编译、`TEST_CASE` 使用等）。

---

## 🗓 3 天测试日程概览

### Day 1 ✅（重点：基础+驱动入门）
- **上午**
  - em_base 快速回归检查（接口稳定性、关键 util 测试）
  - em_test：确认新增断言宏接口（实现/编译）与 `TEST_ENABLE` 集成
- **下午**
  - DHT11：硬件连线校验、读取稳定性测试、错误/超时行为测试
  - DS1302：读写寄存器、BCD 编码/解码验证、掉电恢复检查

**交付物**：每个模块填一份测试记录（见“测试记录模板”），并将 `em_test` 的结构化输出设置为 JSON（若可用，保存到 artifacts）。

### Day 2 ✅（重点：存储类驱动）
- **上午**
  - W25QXX（SPI Flash）：ID 检查、擦除/写/读回验证、跨页/跨扇区边界测试
- **下午**
  - AT24CXX（I2C EEPROM）：页面写边界、随机读写、NACK 与重试、写延时测试

**交付物**：保存读写比对日志与失败示例（十六进制差异），上传 JSON 测试输出。

### Day 3 ✅（重点：测距+回归）
- **上午**
  - TOFxxF：读取测距、量程边界、异常响应、连续测量稳定性
- **下午**
  - 全量回归（快速跑 Day1/Day2 的关键用例）
  - 汇总测试结果、问题分类（阻断/非阻断）、准备发布检查清单

**交付物**：最终测试报告（summary JSON + 人类可读摘要），并给出发布建议（通过 / 延期并列出原因）。

---

## ✅ 单模块测试清单（每项均记录结果）
每个模块使用统一的记录模板：
- 模块：
- 测试者：
- 日期/时间：
- 用例 ID：
- 操作步骤：
- 预期结果：
- 实际结果（PASS/FAIL）：
- 备注 & 需要复现的 log 路径（JSON 或 console 输出）：

---

## 📂 测试结果收集与格式
- 使用 `em_test` 的结构化输出（建议 JSON），环境变量控制：
  - `TEST_OUTPUT=json`
  - `TEST_OUTPUT_FILE=build/artifacts/test_results/v0.2/<module>-<yyyy-mm-dd>.json`
- 对手工测试也保存为结构化条目（同 JSON schema），以便汇总。

---

## ⚠️ 依赖与需要确认的硬件/资源
- 硬件板子或传感器是否齐全（DHT11、TOF、DS1302、W25QXX、AT24CXX）
- 逻辑电平（3.3V/5V）与接口连接方式（SPI/ I2C / 单线）
- 必要工具：示波器 / 逻辑分析仪（建议用于通信异常定位）、USB 转串口、线缆、上位机日志接收工具

> **待确认**：是否有专门的硬件测试平台（自动或手动），以及谁负责提供硬件与接线？

---

## 🛠 风险与缓解措施
- 驱动未在硬件上测试过 → 优先把关键读写/ID 检查作为第一批用例；若失败及时回退发布或标注已知问题。
- em_test JSON 输出跨平台差异 → 同时保留 human-readable 控制台输出以便快速定位。

---

## ✔️ 发布通过标准（最小条件）
- 所有阻断性测试（关键路径）通过，或已记录明确且可接受的缓解措施
- 测试用例覆盖关键行为与错误路径
- 测试结果以 JSON 汇总并附带必要的日志样本

---

## 签收
- 测试负责人：__________________
- 测试完成日期：_____
- 发布决策（通过/延期）及备注：


---

如需，我可以把每一项测试用例转成 `TEST_CASE` 框架下的单元/集成测试，或把此计划同步成 GitHub Issue 列表。