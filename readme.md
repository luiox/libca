libca是一个可裁剪的库，可以选择所需要的部分进行裁剪。

不过要注意的是，所有的都依赖于core模块。

目前分为下面这些模块

core - 核心库，包含了一些必备的东西

event - 事件库，提供一个事件系统

log - 日志库，提供简单易用的日志记录和日志输出

network - 对Windows和Linux下网络api的封装

utility - 实用工具拓展库，包括（ini、json等常见文件解析，一些常用的设计模式辅助类）

database - 数据库orm+连接池的实现


