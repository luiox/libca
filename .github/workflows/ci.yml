name: CI

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: true

    # --- 缓存部分移到安装之前 ---
    - name: Cache xmake packages and build
      uses: actions/cache@v4
      with:
        path: |
          ~/.xmake
          build
        key: ${{ runner.os }}-xmake-v2.9.6-${{ hashFiles('**/xmake.lua') }}
        restore-keys: |
          ${{ runner.os }}-xmake-v2.9.6-
          ${{ runner.os }}-xmake-

    - name: Setup xmake
      uses: xmake-io/github-action-setup-xmake@v1
      with:
        xmake-version: 2.9.6

    - name: Configure xmake
      run: xmake config --toolchain=gcc --ccache=y -y

    - name: Build
      run: xmake build -v

    - name: Run tests
      run: xmake test -g em/test -v
